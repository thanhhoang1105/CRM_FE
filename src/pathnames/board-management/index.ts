const main = {
    name: 'Board',
    path: '/board-management'
};

const edit = {
    name: 'Opportunity edit',
    path: '/board-edit'
};

const boardManagement = {
    main,
    edit
};

export default boardManagement;
